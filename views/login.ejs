<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Climate Solutions</title>
  <link rel="icon" type="image/png" href="/images/ironman.png?v=1">
  <link rel="stylesheet" href="/css/main.css" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</head>
<body class="flex flex-col min-h-screen bg-gray-900 text-gray-300">

  <%- include('partials/navbar', { page: '/login' }) %>

  <main class="container mx-auto py-20 flex-grow space-y-10">

    <section class="bg-gray-800 text-white py-10 px-8 rounded-xl shadow-lg max-w-screen-2xl mx-auto text-center">
      <h1 class="text-4xl font-bold mb-2">Log In</h1>
      <p class="text-gray-400">Please provide your credentials below</p>
    </section>

    <section class="bg-gray-800 text-white py-10 px-8 rounded-xl shadow-lg max-w-screen-2xl mx-auto">
      <form action="/login" method="POST" class="space-y-6">
        <div>
          <label for="userName" class="block text-sm text-gray-400 mb-1">User Name</label>
          <input
            type="text"
            name="userName"
            id="userName"
            placeholder="User Name"
            required
            value="<%= userName %>"
            class="w-full px-4 py-2 rounded-md bg-gray-900 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>

        <div>
          <label for="password" class="block text-sm text-gray-400 mb-1">Password</label>
          <input
            type="password"
            name="password"
            id="password"
            placeholder="Password"
            required
            class="w-full px-4 py-2 rounded-md bg-gray-900 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>

        <div>
          <button
            type="submit"
            class="w-full px-6 py-2 bg-blue-500 hover:bg-blue-600 text-white font-semibold rounded-md transition duration-200"
          >
            Login
          </button>
        </div>
      </form>

      <% if (errorMessage) { %>
        <div class="mt-6 p-4 bg-red-700 text-white rounded-md flex items-center justify-center space-x-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <span><%= errorMessage %></span>
        </div>
      <% } %>
    </section>

  </main>

  <%- include('partials/footer', { page: '/login' }) %>
</body>
</html>
