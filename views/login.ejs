<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Climate Solutions</title>
  <link rel="icon" type="image/png" href="/images/ironman.png">
  <link rel="stylesheet" href="/css/main.css" />
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</head>
<body class="flex flex-col min-h-screen bg-gray-900 text-gray-300">

  <%- include('partials/navbar', { page: '/login' }) %>

  <main class="container mx-auto py-20 flex-grow">
    <section class="bg-gray-800 text-white py-10 px-6 rounded-lg shadow-lg max-w-xl mx-auto text-center">
      <h1 class="text-4xl font-bold mb-6">Login</h1>
      <form action="/login" method="POST" class="space-y-6">
        <div>
          <input type="text" name="userName" placeholder="User Name" required
            value="<%= userName %>"
            class="w-full px-4 py-2 rounded-md bg-gray-900 text-white border border-gray-600" />
        </div>

        <div>
          <input type="password" name="password" placeholder="Password" required
            class="w-full px-4 py-2 rounded-md bg-gray-900 text-white border border-gray-600" />
        </div>

        <div>
          <button type="submit"
            class="w-full px-6 py-2 bg-blue-500 hover:bg-blue-600 text-white font-semibold rounded-md transition">
            Login
          </button>
        </div>
      </form>

      <% if (errorMessage) { %>
        <div class="alert alert-error bg-red-700 text-white flex items-center justify-center mt-6 p-4 rounded-md">
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <span><%= errorMessage %></span>
        </div>
      <% } %>
    </section>
  </main>

  <%- include('partials/footer', { page: '/login' }) %>
</body>
</html>
