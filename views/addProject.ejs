<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Project</title>
    <link rel="icon" type="image/png" href="/images/ironman.png">
    <link rel="stylesheet" href="/css/main.css" />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }

        .dropdown:focus-within .dropdown-content {
            display: block;
            opacity: 1;
            visibility: visible;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #1f2937;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            border-radius: 6px;
            padding: 10px;
            z-index: 100;
            min-width: 150px;
        }

        .dropdown:hover .dropdown-content,
        .dropdown-content:hover {
            display: block;
        }
    </style>
</head>
<body class="flex flex-col min-h-screen bg-gray-900 text-gray-300">
    <%- include('partials/navbar', { page: '/solutions/addProject' }) %>

    <main class="container mx-auto py-10 flex-grow">
        <section class="bg-gray-800 text-white py-10 px-6 rounded-lg shadow-lg">
            <h1 class="text-4xl font-bold text-center">Add Project</h1>
            <p class="mt-2 text-center text-gray-400">Use the below form to add a new Project to Climate Solution Project</p>
        </section>

        <form action="/solutions/addProject" method="POST" class="mt-10 space-y-6">
            <div class="grid md:grid-cols-2 gap-6">
                <div>
                    <label for="title" class="block text-sm font-medium">Title</label>
                    <input type="text" id="title" name="title" required class="mt-1 block w-full rounded-md bg-gray-800 text-white border border-gray-600 px-3 py-2">
                </div>

                <div>
                    <label for="feature_img_url" class="block text-sm font-medium">Feature Image (URL)</label>
                    <input type="url" id="feature_img_url" name="feature_img_url" required class="mt-1 block w-full rounded-md bg-gray-800 text-white border border-gray-600 px-3 py-2">
                </div>

                <div>
                    <label for="intro_short" class="block text-sm font-medium">Intro</label>
                    <textarea id="intro_short" name="intro_short" required rows="2" class="mt-1 block w-full rounded-md bg-gray-800 text-white border border-gray-600 px-3 py-2"></textarea>
                </div>

                <div>
                    <label for="summary_short" class="block text-sm font-medium">Short Summary</label>
                    <textarea id="summary_short" name="summary_short" required rows="2" class="mt-1 block w-full rounded-md bg-gray-800 text-white border border-gray-600 px-3 py-2"></textarea>
                </div>

                <div>
                    <label for="sector_id" class="block text-sm font-medium">Sector</label>
                    <select id="sector_id" name="sector_id" required class="mt-1 block w-full rounded-md bg-gray-800 text-white border border-gray-600 px-3 py-2">
                        <% sectors.forEach(sector => { %>
                            <option value="<%= sector.id %>"><%= sector.sector_name %></option>
                        <% }) %>
                    </select>
                </div>

                <div>
                    <label for="impact" class="block text-sm font-medium">Impact</label>
                    <textarea id="impact" name="impact" required rows="2" class="mt-1 block w-full rounded-md bg-gray-800 text-white border border-gray-600 px-3 py-2"></textarea>
                </div>

                <div class="md:col-span-2">
                    <label for="original_source_url" class="block text-sm font-medium">Original Source (URL)</label>
                    <input type="url" id="original_source_url" name="original_source_url" required class="mt-1 block w-full rounded-md bg-gray-800 text-white border border-gray-600 px-3 py-2">
                </div>
            </div>

            <div class="text-center">
                <button type="submit" class="px-6 py-3 bg-green-500 hover:bg-green-600 text-white font-semibold rounded-md">Add Project</button>
            </div>
        </form>
    </main>

    <%- include('partials/footer', { page: '/solutions/addProject' }) %>
</body>
</html>
