<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Register - Climate Solutions</title>
  <link rel="icon" type="image/png" href="/images/ironman.png">
  <link rel="stylesheet" href="/css/main.css" />
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</head>
<body class="flex flex-col min-h-screen bg-gray-900 text-gray-300">
  <%- include('partials/navbar', { page: '/register' }) %>

  <main class="container mx-auto py-20 flex-grow">
    <section class="bg-gray-800 text-white py-10 px-6 text-center rounded-lg shadow-lg max-w-3xl mx-auto">
      <h1 class="text-4xl font-bold mb-2">Register</h1>
      <p class="text-gray-400 mb-6">Please provide your credentials below</p>

      <% if (successMessage) { %>
        <div class="alert bg-gray-700 border-l-4 border-green-500 mb-4 text-white flex items-center">
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <span><%= successMessage %></span>
        </div>
        <a href="/login" class="btn btn-success bg-green-600 hover:bg-green-700 text-white px-6 py-2 rounded-md">
          Proceed to Log in
        </a>
      <% } else { %>
        <% if (errorMessage) { %>
          <div class="alert alert-error bg-red-700 text-white mb-4 flex items-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span><%= errorMessage %></span>
          </div>
        <% } %>

        <form action="/register" method="POST" class="space-y-4 text-left">
          <div>
            <label for="userName" class="block text-sm font-medium text-gray-300">User Name</label>
            <input type="text" id="userName" name="userName" placeholder="User Name" required
              value="<%= userName %>"
              class="w-full mt-1 px-4 py-2 rounded-md bg-gray-700 text-white border border-gray-600" />
          </div>

          <div>
            <label for="password" class="block text-sm font-medium text-gray-300">Password</label>
            <input type="password" id="password" name="password" placeholder="Password" required
              class="w-full mt-1 px-4 py-2 rounded-md bg-gray-700 text-white border border-gray-600" />
          </div>

          <div>
            <label for="password2" class="block text-sm font-medium text-gray-300">Confirm Password</label>
            <input type="password" id="password2" name="password2" placeholder="Confirm Password" required
              class="w-full mt-1 px-4 py-2 rounded-md bg-gray-700 text-white border border-gray-600" />
          </div>

          <div>
            <label for="email" class="block text-sm font-medium text-gray-300">Email Address</label>
            <input type="email" id="email" name="email" placeholder="Email Address" required
              class="w-full mt-1 px-4 py-2 rounded-md bg-gray-700 text-white border border-gray-600" />
          </div>

          <button type="submit"
            class="bg-green-600 hover:bg-green-700 text-white font-semibold py-2 px-6 rounded-md transition">
            Register
          </button>
        </form>
      <% } %>
    </section>
  </main>

  <%- include('partials/footer', { page: '/register' }) %>
</body>
</html>
